
public class concepts {
	public static void main(String[] args) {
//	//배열 스트림으로 평균
//	Arrays.stream(numbers).average().orElse(0);
//
//	//배열 값넣을시
//	Scanner
//	Scanner i = new Scanner(System.in);
//	I.nextInt();
//
//	//Import shortcut
//	Commend + shift + o
//
//
//	//최대공약수 == gcd
//	//최소공배수 == a*b/gcd
//			int min = (a < b) ? a : b; 
//			int gcd = 0;
//			for (int i = 1; i <= min; i++) {
//				if (a % i == 0 && b % i == 0)
//					gcd = i;
//			}
//
//	//최대값
//	Int max = arr[0];
//
//
//	//Stream 비교
//	    List<Integer> list = Arrays.stream(array).boxed().collect(Collectors.toList());
//	        int max = list.stream().max(Integer::compareTo).orElse(0);
//	        int index = list.indexOf(max);
//
//	//배열 복사 및 자르기
//	String[] nArr = Arrays.copyOfRange(num, 0, index+1);
//
//	//모든Util import
//	import java.util.*;
//	Import java.math.*;
//
//	//정렬
//	Arrays.sort(a);
//
//
//	//문자포함 contains
//	str1.toLowerCase().contains(str2.toLowerCase()));
//
//
//	//mapToInt 배열의 문자 숫자
//	Arrays.stream(strList).mapToInt(String::length).toArray();
//
//	int[] arr ={“1”,”2”};
//	List<Integer> list = new List<>(Arrays.asList(arr));
//
//	//reverse
//	list.sort(Comparator.reverseOrder());
//
//	String reversed=
//	New StringBuffer(str).reverse().toString();
//
//	// List<String> list = Arrays.asList(arr);
//	배열 -> ArrayList 바꾸기 가능 
//
//	//그러나 int의 경우는 형변환이 불가하므로 위의 메소드 사용 불가
//
//	//버림
//	Math.foor(a); 
//	(A/100)*100;
//
//	//절대값
//	Math.abs(a);
//
//	//올림
//	Math.ceil(a);
//
//	//반올림
//	Math.round(a);
//
//	// 반복문은 위에서 부터 계산되어 나온다는 것을 생각
//
//	// Scanner 반복구문찍기
//	Scanner sc = new Scanner(System.in);
//	Int n = sc.nextInt();
//	System.out.prinln(“*”.repeat(i));
//
//	//double x -> Double
//	//Math.sqrt() 제곱근
//	//Math.pow() 제곱수
//
//
//	// << 피연산자의 비트열을 왼쪽으로 이동시키며 이동에 따른 빈공간은 0으로 채움 => 2진법 계산
//	Ex) int num = 2 << 1;
//	00000010 => 00000100
//	2*2 = 4
//
//	//Double을 int로 구현
//	answer.intValue();
//
//	// char -> int 변환
//	Character.getNumericValue();
//
//	// HashMap
//	Hash<String, String> map = new HashMap<>();
//	map.put(“people,”사람”)
//	map.keySet()
//
//
//	// Arrays.copyOf()
//	원본 특정배열 복사 
//
//	// 깊은복사, 얕은복사 정리
//	Deep Copy : ’실제값’을 새로운 메모리 공간에 복사
//	Shallow Copy : ‘주소값’을 복사
//	//StringBuilder
//	append()
//	문자열 뒤에 붙히기
//
//	//import java.math.*;
//	BigDecimal a = new BigDecimal("2.01");
//	a.multiply(new BigDeciamal(1));
//	Long.parseLong(a.devide(1));
//
//
//	Answer = numbers[(2 * (k-1) % numbers.length)];
//
//
//	[
//	[1,2]
//	[3,4]
//	[5,6]
//	[7,8]
//	]
		
//import java.util.stream.Collectors;
//	    중복 제거
//      List<String> newList = list.stream().distinct().collect(Collectors.toList());
//		List string으로 합
//		String answer = String.join("",tmpList);
		

		//소문자, 대문자 증
//String a = "ag";
//a.toCharArray();
//		a.toUpperCase();
//		Character.isUpperCase(0);
	}

}
